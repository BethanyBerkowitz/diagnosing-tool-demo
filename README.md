# diagnosing-tool-demo

Please see the READMEs in the `/backend` and `/frontend` folders to understand how to run this app locally. The backend app should run on port 3000 and the frontend on port 8080. 